interface:
  display_name: "Build Agent"
  short_description: "Execute approved PLAN_READY with minimal verified changes"
  default_prompt: "Use $build-agent to execute the approved PLAN_READY artifact exactly as authorized."
policy:
  allow_implicit_invocation: false
